/**
 * The six-column track shared by the vendor table's header row and its body
 * rows. Lifted from the comp: vendor, profile, workflow, amount, agent state,
 * overflow. Keeping it in one place stops the two grids drifting apart.
 */
export const VENDOR_GRID =
  "grid grid-cols-[minmax(168px,1fr)_minmax(170px,1.05fr)_168px_118px_156px_40px] items-center gap-x-[14px]";
