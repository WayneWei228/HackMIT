# Base URL of the close API (FastAPI). Copy this file to `.env.local`.
# Leave it unset and the app falls back to http://localhost:8000; with no API
# listening there, every screen stays on its synthetic dataset.
NEXT_PUBLIC_API_URL=http://localhost:8000
