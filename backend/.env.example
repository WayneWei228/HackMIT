# Claude access for reasoning agents (falls back to the local `claude` CLI when unset)
ANTHROPIC_API_KEY=
# Jev (TypeSafe) for fast classification; when unset the classifier runs rules-only
TYPESAFE_API_KEY=
# Optional tracing
NEATLOGS_API_KEY=
TRUEUP_DB=trueup.db
